node_modules/
.pnpm-store/
dist/
build/
.turbo/
pnpm-lock.yaml
services/api/supabase/.temp/

# Planning documents are authored by the reviewer. Reformatting them creates
# diff noise in files nobody edits in an editor.
docs/

# Same reasoning, now tracked (BE-W8): AI-collaboration working notes, updated
# directly by whoever is at the machine, not run through an editor's formatter.
.ai-collab/
handoff.md
CLAUDE.md

# Agent-local settings. Git-ignored globally, so it never reaches CI — but Prettier
# still saw it and failed `format:check` locally on a file nobody can commit.
.claude/

# Append-only, with frozen sections. `prettier --write` reformatted a frozen table
# once already. The CI guard in .github/workflows/ci.yml catches a deletion; this
# stops the formatter from making one in the first place.
PROJECT-OVERVIEW.md


# The committed design documents. These are the specification of record, exported
# from Claude Design — reformatting them rewrites the thing screens are built
# against. `support.js` is deliberately absent (see the commit that added them);
# the markup is the spec, the inert press states are not.
docs/design/

# Generated by `next build`, and git-ignored (apps/console/.gitignore). On Windows Next
# writes it with CRLF while .prettierrc.json sets endOfLine: "lf", so `format:check`
# failed locally on a file nobody authors and nobody can commit — while CI, on Linux,
# passed. Found by scripts/ci-local.mjs running build before format:check, in CI's order,
# for the first time. Same reasoning as `.claude/` above.
apps/console/next-env.d.ts
