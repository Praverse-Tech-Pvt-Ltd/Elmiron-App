{
  "name": "@elmiron/mock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mock API server with realistic fixtures. Interface contract I2.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src tests",
    "test": "vitest run"
  },
  "dependencies": {
    "@elmiron/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "zod": "^4.1.12"
  }
}
