/**
 * PLACEHOLDER — the Next.js consoles are Frontend's, from week 1.
 *
 * Backend created the workspace entry, the tsconfig and the dependency on
 * `@fieldforce/core`. Frontend runs `create-next-app` into this directory and deletes
 * this file.
 *
 * The reference below exists only to prove the contract package resolves.
 */

import { CONSENT_OUTCOMES } from '@fieldforce/core';

export const consentOutcomes: readonly string[] = CONSENT_OUTCOMES;
